#include <iostream>
#include <string>
#include <sstream>
#include <map>

using namespace std;

int main() {
	map<string, int> defs;
	string token, line;

	while(getline(cin, line)) {
		stringstream ss;
		ss << line;
		while(ss >> token) {
			
		}
	}
}
